// Rules of Reducers

// Must return any value besides 'undefined'

// Produces 'state' or data to be used inside app
// using only previous state and action

// Must n ot return reach 'out of itself' to decide what value to return
// (reducers are pure)

// Must not muatate its input 'stata' argumnet

export default () => {
  return 123;
};
